apply plugin: 'com.android.library'

android {
    compileSdkVersion 26
    buildToolsVersion "26.0.1"

    defaultConfig {
        minSdkVersion 14
        targetSdkVersion 26
    }
}

dependencies {
    compile project(':ee-x-firebase-core')
    compile 'com.google.firebase:firebase-config:11.0.4'
}
