import { IMessageBridgeImpl } from "./IMessageBridgeImpl";

export class MessageBridgeImplEditor implements IMessageBridgeImpl {
    public call(tag: string, message: string): string {
        return ``;
    }
}